{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --prefix frontend && chmod +x frontend/node_modules/.bin/react-scripts && npm run build --prefix frontend && npm install --prefix server"
  },
  "deploy": {
    "startCommand": "node server/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
