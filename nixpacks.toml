[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = [
  "cd frontend && npm install && ./node_modules/.bin/react-scripts build",
  "cd server && npm install"
]

[start]
cmd = "node server/server.js"
